#
# Travis CI recipe to build, test and push docker images to the Docker Hub.
#

language: generic

os:
  - linux

services:
  - docker

git:
  submodules: true

env:
  global:
  - DOCKER_REPO=christopher102994
  - DOCKER_NAME=docker-base-img-gui

jobs:
  include:
  - stage: Alpine Container
    script:
      - source tools/scripts/load_env_files.sh versions/alpine-3.10.env
      - source tools/scripts/versioning.sh 
      - tools/scripts/make_container.sh
    deploy:
        provider: script
        script: tools/scripts/push_to_registry.sh
        
  - stage: Debian Container
    script:
      - source tools/scripts/load_env_files.sh versions/debian-10.env
      - source tools/scripts/versioning.sh 
      - tools/scripts/make_container.sh
    deploy:
        provider: script
        script: tools/scripts/push_to_registry.sh

  - stage: Ubuntu Container
    script:
      - source tools/scripts/load_env_files.sh versions/ubuntu-18.env
      - source tools/scripts/versioning.sh 
      - tools/scripts/make_container.sh
    deploy:
        provider: script
        script: tools/scripts/push_to_registry.sh

  - stage: Git Tag
    script: 
      - source tools/scripts/versioning.sh
      - tools/scripts/push_git_tag.sh
