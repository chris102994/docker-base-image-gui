#!/bin/bash

# This is the start_app script.
# sed -i "s#APP_COMMAND#<Your App's Command>#g" /app/start_app.sh
# The command above will ensure that the script works as intended.

export HOME=/config

APP_COMMAND &
